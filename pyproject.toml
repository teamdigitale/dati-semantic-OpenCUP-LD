[project]
name = "dati-semantic-opencup-ld"
version = "0.1.0"
description = "Conversione dati OpenCUP e collegati in Linked Data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pybars3==0.9.7",
    "rdflib==7.0.0",
]

[tool.uv]
package = false
